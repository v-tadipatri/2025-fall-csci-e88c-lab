package org.cscie88c.core.week6ppt.slide23

class Sort {
  List(1,3,4,2,5).sorted

  case class User(name: String, age: Int)
  val users = List(User("jane", 20), User("joe", 25))
  //users.sorted

  case class Dog(id: String, breed: String, age: Int)
  val allDogs = List(Dog("1", "lab", 3), Dog("2", "poodle", 5))
  //allDogs.sorted

}
